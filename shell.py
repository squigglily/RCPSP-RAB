def main():
    # import pexpect
    # import RCPSP
    # for i in range(1,2):
    #     for j in range(1, 2):
    #         rcpsp_lab = pexpect.spawn("python3")
    #         rcpsp_lab.sendline("import RCPSP")
    #         rcpsp_lab.sendline("RCPSP.main()")
    #         rcpsp_lab.expect("Welcome to RCPSP-Lab.  Please type 'N' to enter a new dataset or 'E' to work with an existing dataset.  Type 'Q' to quit:")
    #         rcpsp_lab.sendline("N")
    #         rcpsp_lab.expect("Please type the filename (including path) for the project:")
    #         filename = "/home/squigglily/Documents/School Stuff/Grad School/Independent_Study/RCP/J30" + str(i) + "_" + str(j) + ".RCP"
    #         project = "J30" + str(i) + "_" + str(j) + "test"
    #         rcpsp_lab.sendline(filename)
    #         rcpsp_lab.expect("Please type a unique name for this project:")
    #         rcpsp_lab.sendline(project)
    #         rcpsp_lab.expect("Please type the number corresponding with the prioritization rule you would like to use:")
    #         rcpsp_lab.sendline("0")
    import sys

    poop = input("Please type poop:")

    sys.argv
    print(sys.argv)